# OneSelect Backend Production Environment Configuration Template
# ================================================================
# Copy this file to .env and customize for your deployment
# All sensitive values should be replaced with your own secure values

# ============================================================
# SECURITY SETTINGS - CRITICAL
# ============================================================

# SECRET_KEY: Used for JWT token signing and encryption
# MUST be changed in production! Generate with: openssl rand -hex 32
SECRET_KEY=change-this-to-a-random-secret-key-in-production-use-openssl-rand-hex-32

# Token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================================
# ADMIN USER - Created on first run
# ============================================================

# First superuser email (admin account)
FIRST_SUPERUSER=admin@example.com

# First superuser password
# IMPORTANT: Change this immediately after first login!
FIRST_SUPERUSER_PASSWORD=change-this-password

# ============================================================
# DATABASE CONFIGURATION
# ============================================================

# SQLite (default, simplest option for small deployments)
SQLALCHEMY_DATABASE_URI=sqlite:////app/data/oneselect.db

# PostgreSQL (recommended for production)
# SQLALCHEMY_DATABASE_URI=postgresql://user:password@postgres:5432/oneselect

# MySQL/MariaDB
# SQLALCHEMY_DATABASE_URI=mysql+pymysql://user:password@mysql:3306/oneselect

# ============================================================
# CORS CONFIGURATION
# ============================================================

# Allowed origins for CORS (JSON array format)
# Add your frontend URL(s) here
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# ============================================================
# APPLICATION SETTINGS
# ============================================================

# Application name
PROJECT_NAME=OneSelect

# API version prefix
API_V1_STR=/v1

# Frontend URL (used for OAuth redirects and CORS)
FRONTEND_URL=http://localhost:3000

# ============================================================
# OAUTH CONFIGURATION (Optional)
# ============================================================

# Google OAuth Settings
# Get credentials from: https://console.cloud.google.com/
# Leave empty to disable Google OAuth

# Google Client ID
GOOGLE_CLIENT_ID=

# Google Client Secret
GOOGLE_CLIENT_SECRET=

# OAuth redirect URI (update with your domain)
GOOGLE_REDIRECT_URI=http://localhost:8000/api/v1/auth/google/callback

# ============================================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================================
#
# 1. ALWAYS change SECRET_KEY to a randomly generated value
# 2. Use strong passwords for FIRST_SUPERUSER_PASSWORD
# 3. Update BACKEND_CORS_ORIGINS with your actual frontend URL
# 4. Consider using PostgreSQL for production instead of SQLite
# 5. Set up HTTPS/TLS in front of the container (nginx, traefik, etc.)
# 6. Keep this .env file secure (never commit to version control)
# 7. Regularly update the container image to get security patches
# 8. Set up regular database backups
# 9. Monitor container logs for security issues
# 10. Consider using secrets management (Vault, AWS Secrets, etc.)
#
# ============================================================
